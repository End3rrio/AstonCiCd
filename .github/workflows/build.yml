name: Build

on:
    push:
      branches: [ "master" ]

jobs:
  build:

    runs-on: wsl-runner

    steps:
      - uses: actions/checkout@v3
      - name: Build with Maven
        run: mvn -B test --file pom.xml